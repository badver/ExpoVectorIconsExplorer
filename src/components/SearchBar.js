import React from 'react';
import { Text, View } from 'react-native';
import palette from './palette';

export default class SearchBar extends React.PureComponent {
  render() {
    return <Text style={this.props.style}>SEARCH BAR</Text>;
  }
}
